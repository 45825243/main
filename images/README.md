# Images for README

Place the All-purpose cluster configuration screenshot here:

- **all-purpose-cluster-config.png** — Screenshot of the Databricks cluster Configuration tab (General, Performance: runtime, worker type, autoscaling, termination; Summary panel with memory, cores, price).

The README references it as `images/all-purpose-cluster-config.png`.
